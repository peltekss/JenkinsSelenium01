pipeline {
    agent any

    tools {
        // Jenkins üzerinde tanımlı JDK ismini kullanın
        jdk 'JDK21'
    }
    stages {
        stage('Hello') {
            steps {
               bat 'mvn test'
            }
        }
    }
}
